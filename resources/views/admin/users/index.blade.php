@extends('layouts.app')
@section('title', 'کاربران و مجوزها')
@section('page-title', 'کاربران و مجوزها')
@section('page-subtitle', 'ساخت حساب، تعیین پلن، دسترسی و تاریخ انقضا')
@section('page-actions')<a href="{{ route('admin.users.create') }}" class="btn-primary"><x-icon name="plus" class="size-4" /><span class="hidden sm:inline">کاربر جدید</span></a>@endsection
@section('content')<div class="card"><div class="card-header"><form method="GET" class="flex w-full max-w-md gap-2"><div class="relative flex-1"><x-icon name="search" class="pointer-events-none absolute right-3 top-1/2 size-4 -translate-y-1/2 text-slate-400" /><input name="q" value="{{ $search }}" class="form-control pr-10" placeholder="نام یا ایمیل"></div><button class="btn-secondary">جست‌وجو</button></form></div><div class="table-wrap"><table class="data-table"><thead><tr><th>کاربر</th><th>نقش</th><th>پلن</th><th>پایان اعتبار</th><th>دسترسی‌ها</th><th>وضعیت</th><th></th></tr></thead><tbody>@foreach($users as $user)<tr><td><div class="font-bold text-slate-900">{{ $user->name }}</div><div dir="ltr" class="mt-1 text-right text-xs text-slate-400">{{ $user->email }}</div></td><td>{{ $user->role->label() }}</td><td>{{ $user->plan->label() }}</td><td>{{ $user->license_expires_at?->format('Y/m/d') ?? 'بدون انقضا' }}</td><td><div class="flex flex-wrap gap-1">@forelse($user->permissions ?? [] as $permission)<span class="rounded-lg bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-600">{{ ['customers'=>'مشتری','goods'=>'کالا','invoices'=>'فاکتور'][$permission] ?? $permission }}</span>@empty<span class="text-xs text-slate-400">—</span>@endforelse</div></td><td><span @class(['status-badge','status-emerald'=>$user->is_active && $user->hasActiveLicense(),'status-rose'=>!$user->is_active || !$user->hasActiveLicense()])>{{ $user->is_active && $user->hasActiveLicense() ? 'فعال' : 'غیرفعال' }}</span></td><td><a href="{{ route('admin.users.edit', $user) }}" class="table-action">ویرایش</a></td></tr>@endforeach</tbody></table></div><div class="border-t border-slate-100 px-5 py-4">{{ $users->links() }}</div></div>@endsection
